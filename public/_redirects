# Netlify/Cloudflare Pages SPA fallback: serve index.html for client-side routes.
# Real static files (assets, /downloads/*) are served first, so this only catches
# app routes like /about, /founder, /founder/dashboard.
/*    /index.html   200
