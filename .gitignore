node_modules
dist
dist-ssr
*.local

# TypeScript incremental build cache
*.tsbuildinfo

# Large desktop installer served from public/downloads — kept out of git (bloat);
# it is still copied into dist/ at build time and deployed with the site.
public/downloads/*.exe
public/downloads/*.blockmap
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
